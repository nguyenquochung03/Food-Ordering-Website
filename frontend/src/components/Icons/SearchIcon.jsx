import React from 'react'
import { FaSearch } from 'react-icons/fa';

const SearchIcon = ({ color, size }) => {
    return (
        <FaSearch color={color} size={size} />
    );
}

export default SearchIcon