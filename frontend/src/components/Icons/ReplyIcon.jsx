import React from 'react'
import { FaReply } from 'react-icons/fa';

const ReplyIcon = ({ color, size }) => {
    return (
        <FaReply color={color} size={size} />
    )
}

export default ReplyIcon